import 'package:flutter/material.dart';

class ScheduleTripCard extends StatelessWidget{
  const ScheduleTripCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Row(
        children: [
          
        ],
      ),
    );
  }
}